# v2.16.17 发布说明

修正 SQL Server、Oracle、达梦及对应兼容入口的字符串方言输出，正确保留反斜杠和 `N` 前缀，覆盖整句 SQL、片段读取和来源复制。

修复 MySQL 及其兼容入口在字符串字面量改写中的反斜杠转义，避免传入的字符串值改变或生成无效 SQL。

公开 API、公开结构体布局、View JSON 字段和所有权规则不变。

新增字符串回归覆盖 13 个方言入口，包括整句和片段输出、字符串值、来源复制、批内顺序和失败回滚。完整 `make test` 通过。

内置 `libpg_query` 标签：`17-6.2.2`；内置 Jansson 版本：`2.15`。
