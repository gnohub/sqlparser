# v2.11.0 发布说明

`v2.11.0` 增加反解析标识符源拼写保真能力。

## 主要变化

- 未被改写的数据库、schema、表名、列名、别名、函数名、索引名、约束名和 CTE 名称保留输入 SQL 中的标识符拼写。
- 未加引号标识符的原始大小写不再被反解析器统一转换。
- 注释、字符串、bind 和引号标识符中的文本不会被误识别为源标识符。
- patch 新生成的节点不会继承输入 SQL 中其他同名节点的拼写。

## 兼容性

- 公共 API、公共结构体、View JSON 和 Query Graph 保持不变。
- 动态库 ABI 主版本保持为 `libsqlparser.so.0`，ABI 导出检查包含 146 个公共符号。

## 发布验证

- GCC 8.3 严格编译与全量测试
- ASan、UBSan 与 Valgrind 内存检查
- ABI 导出检查
- 单次调用性能与内存分配测试

内置 `libpg_query` 版本：`17-6.2.2`。
